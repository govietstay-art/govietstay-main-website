import type {Metadata} from "next";import {notFound} from "next/navigation";import {getKazakhstanPage,getKazakhstanRelated,kazakhstanSeoPages} from "../../../lib/kazakhstanSeoPages";import KazakhstanPage from "../_seo/KazakhstanPage";export const generateStaticParams=()=>kazakhstanSeoPages.map(p=>({slug:p.slug}));export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=getKazakhstanPage(slug);if(!p)return{};const c=`https://www.govietstay.com/kz/${p.slug}`;return{title:{absolute:p.title},description:p.description,alternates:{canonical:c,languages:{"ru-KZ":c}},robots:{index:true,follow:true},openGraph:{type:"article",url:c,title:p.title,description:p.description,locale:"ru_KZ",siteName:"GoVietStay"}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=getKazakhstanPage(slug);if(!p)notFound();return <KazakhstanPage page={p} related={getKazakhstanRelated(p)}/>}
