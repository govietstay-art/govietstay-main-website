import type {Metadata} from "next";import {notFound} from "next/navigation";import {israelSeoPages,getIsraelPage} from "../../../lib/israelSeoPages";import IsraelPage from "../_seo/IsraelPage";
export function generateStaticParams(){return israelSeoPages.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p:any=getIsraelPage(slug);if(!p)return{};const u=`https://www.govietstay.com/il/${p.slug}`;return{title:{absolute:p.title},description:p.desc,alternates:{canonical:u,languages:{"he-IL":u}},other:{"content-language":"he-IL"}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p:any=getIsraelPage(slug);if(!p)notFound();return <IsraelPage p={p}/>;}